export const selectIsAuthenticatedUser = (state) => state.auth.status;
/*export const selectAuthEmail = (state) => state.auth.userEmail;
export const selectAuthPassword = (state) => state.auth.userPassword;*/

export const selectUser = (state) => state.user;
